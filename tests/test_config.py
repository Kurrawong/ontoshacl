"""test_config.py

Test cases to ensure that config is applied appropriately
via the various channels.

"""

import pytest


@pytest.skip(reason="Not Implemented")
def test_cli_flags():
    """Ensure that config values can be set appropriately from the command line"""
    assert False


@pytest.skip(reason="Not Implemented")
def test_json():
    """Ensure that config values can be set appropriately from a json file via the --config flag"""
    assert False


@pytest.skip(reason="Not Implemented")
def test_precedence():
    """Ensure that cli flags take precedence over JSON config values"""
    assert False
